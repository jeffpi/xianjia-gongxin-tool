# 代码更新日志

## ⭐ 2026-07-22 19:30 — V2.5.38 完全可用版
- 备份：`历史代码文件/工信部数据、语音跟踪统计工具_V2.5.38_CC_完全可用版_20260722-1930.py`
- 基准：V2.5.36（2026-07-22 10:49）
- 验收：**10/10 全部通过**（商店8段+微信大包+FTP无误判）
- 内容：
  1. **去头尾修剪**：FTP/非FTP峰值裁剪全删，段内全部标记
  2. **预FTP清理**：第一个FTP对之前不标业务
  3. **阈值调整**：STORE_L_DL_MIN 500→400, GAP_MERGE 3→2, d≥2→1
  4. **步骤⑤兜底**：状态机失败→先试应用商店匹配
  5. **汇总页公式化**：8个指标全改Excel公式引用详细过程sheet（普通+数组公式）
  6. 版本号 V2.5.36_CC → V2.5.38_CC

## 2026-07-22 18:00
- 文件：工信部数据、语音跟踪统计工具_V2.5.38_CC_20260722-1555.py
- 内容：**修复6个应用商店/微信大包识别bug**
  1. **Bug1 DL回判小文件阈值**（行1407）：`STORE_L_FLOW_MIN`(9000)→`STORE_S_FLOW_MAX`(1500)
  2. **Bug2 未配对FTP不回判小文件**（行779）：补`STORE_S_FLOW_MIN<=dl<=STORE_S_FLOW_MAX`先判小文件
  3. **Bug3 窗口扫描小/大文件分离**（行948-964）：按`_exp`分别处理——小文件预期只标小(≥900)，大文件预期只标大(整段≥9000)，不混切
  4. **Bug4 清理过度误清**（行1433）：商店大文件后清理排除`应用商店|微信`
  5. **Bug5 大文件去上限**（行671）：`dl<=STORE_L_FLOW_MAX`→`dl>=STORE_L_FLOW_MIN`(无上限)
  6. **Bug6 小文件去时长限制**（行673/690）：去掉`d<=5`和`d<=10`
  7. **新增微信大包纯UL累计扫描**（行965+）：UL活跃秒burst累计≥1500即标微信大包发送
  8. **验收**：8/8应用商店时间段✓；FTP无误判✓；微信大包328→647行(SC+LTE)

## 2026-07-22 17:30
- 文件：工信部数据、语音跟踪统计工具_V2.5.38_CC_20260722-1555.py
- 内容：**应用商店窗口扫描架构修复（2处改动）**
  1. **_expect_list多槽位填充**（行913-940）：相邻锚点间gap不再只填BIZ_ORDER下一项，改为轮转推导所有槽位（如微信小包→微信大包→应用商店小→应用商店大），各槽位均匀分配gap时间段。供微信大包扫描等时间查表使用
  2. **应用商店扫描改为gap序贯分配**（行958-1022）：不再使用时间中点查_expect_list（避免相邻burst因时间平分落入同一槽位）。改为对每个gap收集DL burst按时序排列，依次对应gap中的预期槽位进行消费——小文件槽取burst累计≥900切小文件，大文件槽取burst整段累计≥9000标大文件（不先切小文件），非app-store槽位不消费DL burst
  3. **验收**：8个应用商店时间段全部识别正确；大文件burst逐行无小文件污染；小文件burst逐行无大文件混入；轮次9新识别正确

## 2026-07-22 17:00
- 文件：工信部数据、语音跟踪统计工具_V2.5.38_CC_20260722-1555.py
- 内容：**应用商店识别全面修复（4处改动）**
  1. **序列状态机_big_bizs补漏**（行718）：兜底扫描集合补上'应用商店大文件下载'，解决pos错位时大业务段无法救援而误判为未知
  2. **商店窗口扫描重写为"事后查顺序表"**（行901-972）：①状态机识别完后收集所有锚点业务段(FTP/微信等)，按BIZ_ORDER轮转推导每个时间空位"预期轮到"什么业务，构建时间→预期业务顺序表；②全数据扫描DL活跃秒(dl>ul且dl>4)按连续性分段(gap>5s断开)，burst取中点查顺序表，**只有预期轮到应用商店才标**——彻底解决FTP下载被误判为应用商店、且真正应用商店不漏判；③标记规则：累计≥900切小文件→归零后剩余整段累计≥9000标大文件(不切在9000点，跑到段末尾)；纯大文件burst(整段≥9000)也标大文件
  3. **DL流量回判去条件**（行1357）：_dl_only去掉contains('应用商店')，已标记的应用商店核心行不再被回判重新分类(避免大文件段被腰斩成小文件)
  4. **修复NameError崩溃**（行1398）：_merged_prev=False初始化(原仅if块内定义，_bi==0时崩溃)
  5. **验收**：输入文件 联通-数据-786_SHBCX0508两江昆仑大道 8个应用商店时间段(小/大各4)全部识别正确；08:33:39~02的FTP下载不再误判；11880行4.18秒
  6. 版本号 V2.5.37_CC → V2.5.38_CC

## 2026-07-21 19:15
- 文件：工信部数据、语音跟踪统计工具_V2.5.35_CC_20260721-1800.py
- 内容：
  1. **跨平台字体适配**：Mac用PingFang SC，Win用Microsoft YaHei；QSS字号px→pt（DPI无关）；启用AA_EnableHighDpiScaling
  2. **f-string兼容修复**：21处f-string内嵌引号被python-minifier破坏，提取为变量/常量避免语法错误
  3. **左侧4区域QSplitter**：输入文件/参数设置/手机号映射/运行区垂直可拖动调整高度
  4. **手机号/业务表列宽可拖动+持久化**：QHeaderView.Interactive + QSettings保存列宽，关再开保持
  5. **三按钮等宽填满**：开始/取消/关于stretch=1均分，随面板宽度自适应
  6. **按钮样式统一**：淡蓝底色+setFont统一字号
  7. **关于对话框新增鸣谢**：丁先甲，谭润林，赵永川，廖洋（排名不分先后）
  8. **biz_params状态机④.5步**：仅6参数全填才参与判断

## 2026-07-21 18:00
- 文件：工信部数据语音工具_V2.5.35_Win打包.zip
- 内容：
  1. **Windows混淆打包**：python-minifier源码混淆（全局/局部变量名替换、注释移除）+ PyInstaller --key AES字节码加密，双层防护
  2. **打包文件**：混淆后py(1981行/165KB) + requirements.txt(8个依赖) + build.bat(CRLF/无中文/5步流程) + setup.iss(Inno Setup安装包) + hubei_monitor_host.txt(鉴权主机)
  3. build.bat遵循CLAUDE.md Windows打包6条规范（CRLF/无中文/python -m PyInstaller/python --version/cd /d "%~dp0"/不用py）
  4. setup.iss参考700M V1.10.04模板（PrivilegesRequired=lowest/x64+arm64/lzma2压缩）
  5. **修复**：f-string内嵌单引号条件表达式被python-minifier转双引号导致语法错误（`✓`→`[OK]`/`[NO]`提取为变量）

## 2026-07-21 10:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.30_CC_20260721-1000.py
- 内容：
  1. **业务顺序表格化**：QListWidget→QTableWidget(7列含辅助参数)，QTableWidgetItem替代QLineEdit(防焦点抢夺)，多选上移下移文本互换
  2. **biz_params辅助参数**：UI读取→WorkerThread→process()→状态机④.5步（流量总和±冗余%+时长±冗余%），仅6参全填参与判断
  3. **参数设置UI横排重构**：QCI/削峰/FTP一行、时间+勾选框一行、业务表格占满宽（ScrollBarAlwaysOff禁横向滚动）
  4. **手机号映射紧凑化**：左右并排去stretch（手动表固定、拖入框可伸展），去括号长说明，列宽Fixed禁拖动
  5. **日志80→100px**：释放空间给参数区；去掉"运行"和"运行结果"标题
  6. **QCI字符串兼容**：加载阶段pd.to_numeric转换；冗余%列缩窄42px
  7. 版本号 V2.5.29 → V2.5.30

## 2026-07-21 09:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.29_CC_20260721-0900.py
- 内容：
  1. **参数设置UI重构**：左右双栏（左栏QCI/削峰/时间/FTP、右栏方案按钮+业务顺序），FTP时长上移、时间左右并排；手机号映射左右并排
  2. **去粘贴映射括号长文**、QCI字符串兼容修复
  3. 版本号 V2.5.28 → V2.5.29

## 2026-07-20 21:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.28_CC_20260720-2100.py
- 内容：
  1. **削峰TOP10%先裁剪再排序**：rates.clip(upper=lim)后再取前10%求均值；批注公式同步修正
  2. 版本号 V2.5.27 → V2.5.28

## 2026-07-20 20:30
- 文件：用户级数据、语音跟踪统计工具_V2.5.27_CC_20260720-2030.py
- 内容：
  1. **序列状态机业务识别**：BIZ_ORDER驱动5轮判断（严格预期→严格首业务→宽松预期→宽松首业务→方向兜底），解决固定规则顺序误判和遗漏
  2. 版本号 V2.5.26 → V2.5.27

## 2026-07-20 20:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.26_CC_20260720-2000.py
- 内容：
  1. **cb()应用商店大检查提到FTP之前** + BIZ_ORDER上下文修正，解决应用商店大被FTP时长误判
  2. 版本号 V2.5.25 → V2.5.26

## 2026-07-20 19:30
- 文件：用户级数据、语音跟踪统计工具_V2.5.25_CC_20260720-1930.py
- 内容：
  1. **业务顺序快速方案切换**：方案一/方案二按钮，点击即切换QListWidget，当前方案按钮高亮
  2. 版本号 V2.5.24 → V2.5.25

## 2026-07-20 19:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.24_CC_20260720-1900.py
- 内容：
  1. **cb()加fallback兜底分类**：三段规则（FTP/应用商店大/微信大）全不命中时按dl/ul方向兜底（dl>ul且dl≥100→应用商店小文件下载，ul>dl且ul≥40→微信小包发送），解决4秒高下载突发等短段遗漏
  2. **fallback段补轮次**：cb()新增的fallback段按时间范围落入最近FTP对的轮次，避免有业务无轮次被过滤
  3. 版本号 V2.5.23_CC → V2.5.24_CC

## 2026-07-20 17:30
- 文件：用户级数据、语音跟踪统计工具_V2.5.23_CC_20260720-1730.py
- 内容：
  1. **基准列J~P按时间范围传播**：Step 4改为`_time_mask = (_t >= b_start) & (_t <= b_end)`按时间传播，覆盖段内所有行含补秒行
  2. **统一基准文件加载路径**：用户传入和默认永川统一走`_build_base_segments`逻辑，自动检测sheet（整理后文件→呼叫详情→首sheet）
  3. **放宽速率校验阈值**：0.3~3.0 → 0.05~20.0，大幅提升对齐率
  4. 版本号 V2.5.22_CC → V2.5.23_CC

## 2026-07-20 15:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.22_CC_20260720-1500.py
- 内容：
  1. **修复基准列J~P仅首行填充bug**：MMF业务段构建时跳过补秒空行（`biz==''`时`continue`而非触发段分裂），确保段范围覆盖段内所有行（含补秒行），使基准列逐行赋值不再被分割成单行段
  2. 版本号 V2.5.21_CC → V2.5.22_CC

## 2026-07-20 14:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.21_CC_20260720-1400.py
- 内容：
  1. **分业务RLC速率汇总改为横表**：联通/电信各独立列（业务/下行RLC吞吐率(Mbps)/上行RLC吞吐率(Mbps)），数值精确到0.01
  2. **详细过程基准列填充**：J~P列（基准_速率类指标、基准_数值、基准_时长类指标、基准_数值.1）填入实际基准数据：速率类指标根据DL/UL方向填"下行/上行RLC吞吐率(Mbps)"，数值取永川逐秒RLC均值，时长类指标填"业务时长(秒)"及段秒数
  3. 版本号 V2.5.20_CC → V2.5.21_CC

## 2026-07-20 13:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.20_CC_20260720-1300.py
- 内容：
  1. **新增「分业务RLC速率汇总」Sheet**：按业务类型统计RLC吞吐率均值（bps→Mbps），上行类业务取Uplink RLC Throughput(bps)、下行类业务取Downlink RLC Throughput(bps)；短名称映射（微信大包发送→微信大包、应用商店大文件下载→应用宝大包等）；跨运营商合并统计
  2. 版本号 V2.5.19_CC → V2.5.20_CC

## 2026-07-20 01:00
- 文件：用户级数据、语音跟踪统计工具_V2.5.16_20260720-0100.py
- 内容：
  1. **修复卡顿问题**：数据业务process()中detail_dict['手机号']构建方式改为预构建映射+列表推导，避免每行遍历data_file_info字典的O(n*m)性能问题，加cancel_check检查点
  2. **修复S1严重问题**：语音VQI的12个数据明细sheet中手机号字段取值错误（voice_records/vqi_records中无'手机号'字段），改为预构建trace_to_phone映射后实时查询
  3. **补充关于对话框更新记录**：V2.5.15/V2.5.16更新记录补充完整（原只写了2项，现覆盖全部11项改动）
  4. **版本号更新**：V2.5.15 → V2.5.16，6处全部更新
  5. 文件名更新时间戳为20260720-0100
- 文件：用户级数据、语音跟踪统计工具_V2.5.11_20260719-1600.py
- 内容：
  1. **修复手机号映射合并问题（Task #31）**
     - `_sync_phone_mapping()`中，QTextEdit处理完成后重新读取QTableWidget行到`phone_trace_map`
     - 确保手动添加和拖入/粘贴的手机号映射都能参与计算
  2. **优化语音VQI批注性能（Task #32）**
     - 在批注循环前预构建运营商和文件名的过滤缓存（_op_voice_cache等6个字典）
     - 修改`_build_anno_text`、`_build_file_anno_text`、`_build_op_anno_text`三个函数签名，增加cache参数
     - 使用缓存查找替代每个单元格都遍历全量records，大幅减少运营商/文件名批注的卡顿
  3. **QCI 6和7改为独立可选不再互斥**
     - 删除`cb6.toggled`和`cb7.toggled`的互斥连接
     - 现在可以同时勾选6和7，或单独勾选任一
  4. **处理开始时清空结果数据显示区**
     - `run()`方法中增加`tbl_detail/tbl_summary/tbl_compare`的setRowCount/setColumnCount清零
     - `vqi_run()`方法中增加`vqi_tbl_raw/vqi_tbl_voice/vqi_tbl_mos/vqi_tbl_data_raw`的清零
     - 日志区也同步清空
  5. **数据业务处理跳过非MMF文件名**
     - `process()`函数中，遇到`ALL_FTP_DETAIL_`开头的文件名直接跳过，不读取不处理
     - 对应的`data_file_info`构建也跳过这些文件
  6. **修复data_file_info索引错位bug（重要）**
     - 原代码用`dfs[fi]`索引，但`dfs`因跳过文件比`files`短，导致IndexError或数据错位
     - 改用独立`dfs_idx`变量跟踪`dfs`中的实际索引，跳过文件时不自增
  7. **增加空dfs边界保护**
     - 当所有文件均为非MMF文件名(ALL_FTP_DETAIL_)时，dfs为空，原`pd.concat(dfs)`会抛ValueError崩溃
     - 新增`if not dfs:`检查，生成提示性空输出文件并return，避免崩溃
  8. **版本号更新**
     - V2.5.10 → V2.5.11，6处全部更新
     - 关于对话框新增V2.5.11更新记录（含data_file_info索引bug修复和空数据保护说明）
     - 文件名更新时间戳为20260719

## 2026-07-19 15:40
- 文件：用户级数据、语音跟踪统计工具_V2.5.10_20260719-1540.py
- 内容：
  1. **新增手机号↔Trace ID映射功能**
     - 主界面新增手机号映射面板（QTableWidget手动添加 + QTextEdit拖拽/粘贴）
     - 支持拖拽导入Excel/txt文件或粘贴文本，自动识别列顺序（表头关键词或内容格式检测）
     - 默认TXT分隔符为Tab，支持一键清空
     - 从文件路径提取`用户跟踪ID=xxx`精确匹配手机号映射表（553匹配，55不匹配）
     - 速率统计Tab和语音VQI Tab共享同一套映射数据

  2. **结果文件新增手机号列**
     - 按文件名sheet：在文件名列后添加手机号列
     - 按运营商sheet：在运营商列后添加手机号列（多个文件用顿号分隔去重）
     - 汇总-数据和语音sheet：在运营商列后添加手机号列（多个文件用顿号分隔去重）

  3. **修复速率统计Tab输出文件无手机号列（致命bug）**
     - 修复`process()`函数中`data_file_info`未定义导致的NameError
     - 在process()中新增data_file_info字典构建逻辑（参考process_vqi()实现）
     - 包含dl_peak/dl_avg/ul_peak/ul_avg/row_count/time_range/operator/full_path字段
     - 汇总sheet已包含手机号列（_calc_summary中手机号收集逻辑已实现）

  4. **更新需求文档.md至V2.5.10**
     - 完整重写需求文档，覆盖速率统计、语音VQI、手机号映射等全部功能点
     - 新增版本历史表格

  5. **版本号更新**
     - V2.5.9 → V2.5.10，6处全部更新
     - 关于对话框新增V2.5.10更新记录
     - 文件名更新时间戳为20260719
- 文件：用户级数据、语音跟踪统计工具_V2.5.9_20260719-1225.py
- 内容：
  1. **修复运营商循环硬编码**
     - 原3处`for op in ['电信', '联通']:`硬编码循环，导致`_detect_operator()`返回'未知'的文件被跳过不纳入统计
     - 改为动态获取数据中实际存在的运营商列表（使用set去重后sorted排序）
     - 修改位置：process_vqi函数第1597行（数据运营商标识）、第1962行（按运营商统计）、第2077行（汇总-数据和语音）
     - 光环578用户（路径无运营商关键字）的语音文件现在能被正确纳入统计，运营商显示为'未知'

  2. **版本号更新**
     - V2.5.8 → V2.5.9，6处全部更新
     - 关于对话框新增V2.5.9更新记录
     - 文件名更新时间戳为20260719
- 文件：用户级数据、语音跟踪统计工具_V2.5.8_20260718-2015.py
- 内容：
  1. **性能优化：基准对齐使用二分查找**
     - 原实现：对每条基准记录遍历所有时间戳计算abs差值，复杂度O(n*m)
     - 新实现：使用pandas的searchsorted进行二分查找，复杂度O(n + m*log n)
     - 大幅提升大文件（19M+）的处理速度

  2. **新增功能：服务小区平均RSRP统计**
     - 新增`_extract_serv_rsrp()`函数，从Detail Info提取servRSRP值
     - 支持格式：`servRSRP=-96,-91`（双值取最大）或`servRSRP=-96`（单值）
     - 筛选Message Type为`RRC_MEAS_RPRT`的行提取RSRP值
     - 按文件名、按运营商、汇总表均增加"服务小区平均RSRP(dBm)"列
     - 新增rsrp_records列表记录每个RSRP值及其_source_file

  3. **自动打开结果文件**
     - 速率统计工具和VQI工具处理完成后自动调用`os.system(f'open "{path}"')`打开Excel

  4. **版本号更新**
     - 所有版本号从V2.5.7更新为V2.5.8
     - 关于对话框新增V2.5.8更新记录

## 2026-07-18 20:02
- 文件：用户级数据、语音跟踪统计工具_V2.5.7_20260718-2000.py
- 内容：
  1. **修复按文件名统计表显示0的核心问题**
     - 在读取语音文件时添加`df_i['_source_file'] = filename_base`字段
     - 创建`voice_file_info`字典记录每个文件的operator、row_count、time_range、time_start、time_end
     - 在voice_records中添加`_source_file`字段（来源于df_msg.loc[pos].get('_source_file', '')）
     - 在vqi_records中添加`_source_file`字段（来源于row.get('_source_file', '')）

  2. **重构按文件名统计逻辑**
     - 从遍历voice_files列表改为遍历voice_file_info字典
     - 使用`[vr for vr in voice_records if vr.get('_source_file') == fname_base]`精确匹配每个文件的轮次
     - 使用`[r for r in vqi_records if r.get('_source_file') == fname_base]`精确匹配每个文件的MOS记录
     - 文件类型直接标记为"语音"（因为只有voice_file_info中的文件才会进入此统计）

  3. **版本号更新**
     - docstring、version变量（3处）、窗口标题、关于对话框版本号、更新记录全部更新为V2.5.7
