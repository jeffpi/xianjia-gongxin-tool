# 先甲工信部工具 - 需求文档

## 基本信息
- 工具名称：用户级数据、语音跟踪统计工具
- 当前版本：**V2.5.39_CC**
- 主脚本：/Users/sun/ClaudeCode/先甲工信部工具/PY文件/工信部数据、语音跟踪统计工具_V2.5.39_CC_20260723-0200.py
- 基准版本：基于 V2.5.36（2026-07-22 10:49），应用商店+微信大包识别均正确
- 备份：历史代码文件/工信部数据、语音跟踪统计工具_V2.5.38_CC_完全可用版_20260722-1930.py

## ⭐ 完全可用版 验收标准（10/10）
输入文件：联通-数据-786_SHBCX0508两江昆仑大道_用户级公共监控_20260722_115718.xlsx
1. 11:03:27-29 应用商店小文件 ✓   2. 11:04:09-21 应用商店大文件 ✓
3. 11:09:04-06 应用商店小文件 ✓   4. 11:09:46-57 应用商店大文件 ✓
5. 11:14:34-37 应用商店小文件 ✓   6. 11:15:17-27 应用商店大文件 ✓
7. 11:20:07-10 应用商店小文件 ✓   8. 11:20:49-59 应用商店大文件 ✓
9. 10:56:30-57 微信大包发送 ✓     10. 08:33:39-02 FTP下载(无误判) ✓

## 历史需求记录

### 2026-07-22（最终版 — 完全可用）
1. **回归V2.5.36识别逻辑**：窗口扫描方案效果不如原版，直接恢复V2.5.36状态机+小业务检测方案（10/10验收全过）
2. **去头尾修剪**：FTP峰值25%裁剪、非FTP子段掐首尾、FTP配对长短边对齐等全部删除，段内所有有流量的行统一标记
3. **预FTP清理**：第一个FTP对开始前不标记任何业务（用`pi[0][2]`参照）
4. **阈值调整**：STORE_L_DL_MIN 500→400、GAP_MERGE 3.0→2.0、应用商店小文件d≥2→1
5. **步骤⑤兜底**：状态机分类失败时先尝试应用商店严格/宽松匹配，不再直接标未知
6. **汇总页全部公式化**：8个指标（阈值占比/平均速率/削峰均值/TOP10%/时长均值/中位值）全部改为Excel公式引用详细过程sheet，支持微调详细过程后自动重算

### 2026-07-21
1. **业务顺序表格化**：QListWidget改为QTableWidget(7列:业务+流量从/到/冗余%+时长从/到/冗余%)，支持多选上移下移、方案一/二切换（辅助参数不清空），业务名双击编辑
2. **辅助参数biz_params**：用户可自定义各业务流量总和和时长限定范围（含上下冗余%），仅6参数全填才参与状态机冲突判断（④.5步）
3. **参数设置UI重构**：参数横排顶部（QCI/削峰/FTP时长一行、时间+勾选框一行）、快速方案+业务顺序右移占满宽
4. **手机号映射左右并排**：手动表格固定宽+拖入框可伸展，去掉粘贴映射括号长说明，列宽禁止拖动
5. **上移下移多行+防崩溃**：QTableWidgetItem文本互换（不移动widget），support多行同时移动
6. **QCI字符串兼容**：加载阶段pd.to_numeric转换，避免QCI字符串导致isin失败

### 2026-07-20
1. **分业务RLC速率汇总Sheet**：按业务类型统计RLC吞吐率均值（bps→Mbps），上行类业务（FTP上传/微信大小包）取Uplink RLC，下行类业务（FTP下载/应用商店大小包）取Downlink RLC，短名称映射，跨运营商合并
2. **RLC汇总表格式**：横表格式，联通和电信各独立列（联通业务/下行/上行 + 电信业务/下行/上行），精确到小数点后2位
3. **详细过程基准列填充**：J-P列（基准_业务类型到基准_数值.1）应填入实际基准数据而非空值/0
4. **基准对齐算法**：改为业务类型序列匹配（构建MMF业务段，贪心前向序列对齐，速率辅助校验0.3~3.0）
5. **补秒**：基准对齐前按秒补齐（pd.date_range + pd.concat），确保每秒都有一行
6. **FTP阈值**：FTP下载/上传的段边界使用>10Mbps阈值（而非>0），避免首尾低速率干扰
7. **业务执行顺序**：界面上移/下移按钮调整BIZ_ORDER，替代拖拽排序
8. **数值格式**：下载速率/上传速率列用num_format='0'显示整数
9. **性能优化**：write_row批量写入替代逐格写，DataFrame碎片化修复
10. **永川基准加载**：解析「整理后文件」sheet，映射联通业务→业务类型列，按连续标签分组成段

### 2026-07-19
1. 完整通话规则改为只看BYE
2. RSRP拆分为(全部)和(通话时)两列
3. 批注增加每轮次明细
4. 汇总表/按文件名表列顺序调整
5. 运营商检测增加QCI兜底
6. 数据业务写入改用xlsxwriter
7. 手机号列构建性能优化
8. 取消响应优化
9. 手机号-TraceID映射持久化
10. QCI 6和7独立可选

### 2026-07-18 及之前
1. 5G用户级速率统计
2. 语音VQI跟踪统计
3. MMF自动识别业务类型
4. 方向判定用有效活跃秒数
5. 网络覆盖sheet
6. 在线更新方案
