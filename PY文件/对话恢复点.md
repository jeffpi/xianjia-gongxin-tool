# 当前对话状态 - 5G用户级公共监控速率统计工具

## 文件信息
- **主脚本路径**: `/Users/sun/ClaudeCode/先甲工信部工具/5G用户级公共监控速率统计工具-V2.0.0.py`
- **当前版本**: 代码中已改为 V2.0.9
- **工作目录**: `/Users/sun/ClaudeCode/先甲工信部工具/`
- **输出文件**: `联通/` 目录下

## 最近修改的内容（V2.0.9）

### 对比横表（对比Sheet和对比横表Sheet）完全重写
修改了对比数据生成逻辑，位置在文件中的对比模块（约第384行开始）。

### 新逻辑要点：
1. **代码业务**：按轮次分组，每个轮次每种业务取第一个
2. **基准业务**：按180秒时段去重，同类型只保留第一个，然后每6个为一组
3. **匹配**：代码轮次和基准轮次按时间差<300秒匹配，双方都完整（6种业务）才标记轮次
4. **输出**：按业务类型固定顺序，基准和代码时间早的在前

### 上次运行错误
`UnboundLocalError: cannot access local variable 'REQ_BIZ'` - 已修复
（函数内BIZ_ORDER和全局REQ_BIZ变量名冲突，已改为BIZ_ORDER）

## 待完成
1. **运行脚本**：`python3 "5G用户级公共监控速率统计工具-V2.0.0.py" --cmd`
2. **验证对比横表格式**是否符合用户要求的样式
3. 用户给的案例样式（关键要求）：
   - 每列一个来源（代码或基准）
   - 时间早的在前
   - 缺失标记"缺失"
   - 完整才标轮次，否则"不计轮次"
   - 第1行和第3行合并单元格

## 恢复位置
- 函数 `process()` 中的对比模块（约384-520行）
- 关键变量：`code_items`、`base_items`、`code_rounds`、`base_rounds`、`cr`